import { InputValidatorDirective } from './input-validator';

describe('LogInDirective', () => {
  it('should create an instance', () => {
    const directive = new InputValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
